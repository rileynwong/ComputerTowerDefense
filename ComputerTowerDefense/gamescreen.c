/* File:    
   Author: Nancy Wong
   Desc:
*/

#include "gamescreen.h"
#include <vector>

void DrawInit(vector< vector<char> > *GameScreen) {

  /* draw path */
 
}

void DrawInit(vector< vector<int> > *GameScreen) {
  int row, col;
  for(row = 0; row < VEC_L; row++) {
//    GameScreen->at(row) = new vector<int> (VEC_W, 0);
  }
}

void Refresh(vector< vector<int> > *GameScreen) {
}

